spring:
  datasource:
      username: root
      password: root
      url: jdbc:mysql://localhost:3306/dnb2
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    show-sql: true
server:
  port: 9000